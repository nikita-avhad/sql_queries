CREATE DATABASE school1;
USE school1;
CREATE TABLE dept(
id INT PRIMARY KEY,
name VARCHAR(20)
);

INSERT INTO dept
VALUES
(101,"ENGLISH"),
(102,"IT");

UPDATE DEPT
SET id =103
WHERE id = 102;

SELECT * FROM dept;

CREATE TABLE techer(
id INT PRIMARY KEY,
name VARCHAR(25),
dept_id INT,
FOREIGN KEY (dept_id) REFERENCES dept(id)
ON UPDATE CASCADE
ON DELETE CASCADE
);


INSERT INTO techer
VALUES
(101,"ADAM",101),
(102,"EVE",102);

SELECT * FROM techer;
